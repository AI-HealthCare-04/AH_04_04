# docker
DOCKER_USER=ah0404
DOCKER_REPOSITORY=senior-challenge

# fastapi
APP_VERSION=v1.0.0
APP_EXPOSE_PORT=8000
SECRET_KEY=change-me-prod-secret
COOKIE_DOMAIN=localhost
SUPPORT_EMAIL=support@CHANGE-ME.com

# db
DB_HOST=mysql
DB_PORT=3306
DB_EXPOSE_PORT=3306
DB_USER=ozcoding
DB_PASSWORD=Password1234@
DB_ROOT_PASSWORD=Ozcoding1234@
DB_NAME=ai_health

# nginx
NGINX_EXPOSE_PORT=80
NGINX_SSL_EXPOSE_PORT=443

# 운동 영상(#72/#199) 정적 호스트 루트. 앱은 {BASE_URL}/videos/{파일명} 으로 스트리밍한다.
#   실제 배포 값은 PROD_ENV 시크릿에 넣어야 재배포에도 유지된다(여긴 예시).
EXERCISE_VIDEO_BASE_URL=https://aigo-health.duckdns.org
